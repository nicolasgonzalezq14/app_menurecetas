// Clase que contiene nombres de rutas para la aplicación.
class AppRouteName {
  // Constante que representa la ruta para comenzar.
  static const String getStarted = "/get-started";

  // Constante que representa la ruta para la página principal (home).
  static const String home = "/home";
}
